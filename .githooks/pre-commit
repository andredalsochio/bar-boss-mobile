#!/bin/bash

# Hook de pré-commit para Agenda de Boteco
# Executa verificações de qualidade antes de permitir o commit

set -e

echo "🚀 Executando verificações pré-commit..."
echo ""

# Cores para output
RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m' # No Color

# Verificar se estamos no diretório correto
if [ ! -f "pubspec.yaml" ]; then
    echo -e "${RED}❌ Erro: Execute este hook no diretório raiz do projeto Flutter${NC}"
    exit 1
fi

# 1. Verificar formatação do código
echo -e "${BLUE}1. Verificando formatação do código...${NC}"
if ! dart format --set-exit-if-changed lib/ test/ > /dev/null 2>&1; then
    echo -e "${RED}❌ Código não está formatado corretamente${NC}"
    echo "Execute: dart format lib/ test/"
    exit 1
else
    echo -e "${GREEN}✅ Formatação OK${NC}"
fi

# 2. Executar análise estática
echo -e "${BLUE}2. Executando análise estática...${NC}"
if ! flutter analyze --no-pub > /dev/null 2>&1; then
    echo -e "${RED}❌ Análise estática falhou${NC}"
    echo "Execute: flutter analyze"
    exit 1
else
    echo -e "${GREEN}✅ Análise estática OK${NC}"
fi

# 3. Executar lints customizados
echo -e "${BLUE}3. Executando lints customizados...${NC}"
if [ -f "scripts/check_custom_lints.sh" ]; then
    if ! ./scripts/check_custom_lints.sh; then
        echo -e "${RED}❌ Lints customizados falharam${NC}"
        exit 1
    fi
else
    echo -e "${YELLOW}⚠️  Script de lints customizados não encontrado${NC}"
fi

# 4. Verificar se há arquivos gerados pelo build_runner
echo -e "${BLUE}4. Verificando arquivos gerados...${NC}"
GENERATED_FILES=$(find lib/ -name "*.g.dart" -o -name "*.freezed.dart" | wc -l | tr -d ' ')
if [ "$GENERATED_FILES" -eq 0 ]; then
    echo -e "${YELLOW}⚠️  Nenhum arquivo gerado encontrado. Execute: flutter packages pub run build_runner build${NC}"
else
    echo -e "${GREEN}✅ Arquivos gerados encontrados ($GENERATED_FILES)${NC}"
fi

# 5. Verificar se há TODOs ou FIXMEs em arquivos staged
echo -e "${BLUE}5. Verificando TODOs/FIXMEs...${NC}"
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.dart$' || true)
if [ -n "$STAGED_FILES" ]; then
    TODOS=$(echo "$STAGED_FILES" | xargs grep -l "TODO\|FIXME\|XXX" || true)
    if [ -n "$TODOS" ]; then
        echo -e "${YELLOW}⚠️  Arquivos com TODO/FIXME encontrados:${NC}"
        echo "$TODOS" | while read -r file; do
            echo "  $file"
        done
        echo "  💡 Considere resolver antes do commit"
    else
        echo -e "${GREEN}✅ Nenhum TODO/FIXME encontrado${NC}"
    fi nelse
    echo -e "${GREEN}✅ Nenhum arquivo Dart staged${NC}"
fi

# 6. Verificar tamanho do commit
echo -e "${BLUE}6. Verificando tamanho do commit...${NC}"
FILES_CHANGED=$(git diff --cached --name-only | wc -l | tr -d ' ')
if [ "$FILES_CHANGED" -gt 20 ]; then
    echo -e "${YELLOW}⚠️  Commit muito grande ($FILES_CHANGED arquivos). Considere dividir em commits menores.${NC}"
else
    echo -e "${GREEN}✅ Tamanho do commit OK ($FILES_CHANGED arquivos)${NC}"
fi

echo ""
echo -e "${GREEN}🎉 Todas as verificações pré-commit passaram!${NC}"
echo "Prosseguindo com o commit..."
echo ""